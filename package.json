{
  "name": "voltaire-resolver-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],

  "scripts": {
    "build:resolver": "pnpm --filter voltaire-resolver run build",
    "build:extension": "pnpm --filter voltaire-resolver-extension run build"
  }
}